linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused

issues:
  exclude-rules:
    # Test files: allow unchecked errors on common test helper patterns
    - path: _test\.go
      linters:
        - errcheck
